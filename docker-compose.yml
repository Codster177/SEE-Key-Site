version: "3.9"

services:
  web:
    image: codster177/see-ket-site:latest
    ports:
      - "127.0.0.1:3000:3000"
    volumes:
      - sqlite_data:/app/data
    environment:
      - DATABASE_URL=file:/app/data/dev.db
    restart: always

volumes:
  sqlite_data: